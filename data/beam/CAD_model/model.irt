include("dims.irt");
TV = GBOX(
    point(0,0,0),
    point(0,0,Height),
    point(0,Width,0), 
    point(0,Width,Height),
    point(9,0,0),
    point(9,0,Height),
    point(9,Width,0),
    point(9,Width,Height)
);
save("model.itd", list(TV));
V = TVolume(TV, 1);
OUT_FILE = FPrintFile( "props.txt" );
fprintf(OUT_FILE, "volume %lf\\n", list(V));
free(TV);
exit();
