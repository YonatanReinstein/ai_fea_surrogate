include("dims.irt");
TV = GBOX(
    point(0,0,0),
    point(0,0,Width),
    point(0,Height,0), 
    point(0,Height,Width),
    point(9,0,0),
    point(9,0,Width),
    point(9,Height,0),
    point(9,Height,Width)
);
save("model.itd", list(TV));
V = TVolume(TV, 1);
OUT_FILE = FPrintFile( "props.txt" );
fprintf(OUT_FILE, "volume %lf\\n", list(V));
free(TV);
exit();
