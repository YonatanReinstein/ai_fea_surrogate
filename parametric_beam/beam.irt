include("dims.irt");
TV = GBOX(
    point(0,0,0), point(0,0,Width),
    point(0,Height,0), point(0,Height,Width),
    point(Length,0,0), point(Length,0,Width),
    point(Length,Height,0), point(Length,Height,Width)
);
save( "beam.itd", list(TV));
free(TV);
exit();
