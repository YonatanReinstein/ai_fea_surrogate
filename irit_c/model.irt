include("dims.irt");
TV = GBOX(
    point(0,0,0),
    point(0,0,1),
    point(0,1,0), 
    point(0,1,1),
    point(1,0,0),
    point(1,0,1),
    point(1,1,0),
    point(1,1,1)
);
save("wing.itd", list(TV));
V = TVolume(TV, 1);
OUT_FILE = FPrintFile( "props.txt" );
fprintf(OUT_FILE, "volume %lf\\n", list(V));
free(TV);
exit();
